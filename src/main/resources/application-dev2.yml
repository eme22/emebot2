quarkus:
  datasource:
    db-kind: h2
    username: sa
    password: sa
    jdbc:
      url: jdbc:h2:file:./target/h2db
  hibernate-orm:
    database:
      generation: update
    unsupported-properties:
      "hibernate.enable_lazy_load_no_trans": true