quarkus:
  datasource:
    db-kind: mariadb
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}
    jdbc:
      url: jdbc:mariadb://192.168.18.164:3306/embot
  hibernate-orm:
    log:
      sql: false
    database:
      generation: update
    unsupported-properties:
      "hibernate.enable_lazy_load_no_trans": true
